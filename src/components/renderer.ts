import IUnitLibComponents from "../interfaces/IUnitLibComponents";
import UnitBehaviour from "../unitBehaviour";

export default abstract class Renderer extends UnitBehaviour{
    public alpha: number=1.0;
}